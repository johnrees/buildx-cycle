{
  "name": "buildx-cycle",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "cycle-scripts": "1.1.0",
    "ghpages": "^0.0.10"
  },
  "dependencies": {
    "@cycle/dom": "^17.6.0",
    "@cycle/most-run": "^7.1.0",
    "@cycle/run": "^3.1.0",
    "@most/dom-event": "^1.3.2",
    "aframe": "^0.6.0",
    "most": "^1.5.0",
    "xstream": "^10.9.0"
  },
  "scripts": {
    "start": "cycle-scripts start",
    "test": "cycle-scripts test",
    "build": "cycle-scripts build",
    "eject": "cycle-scripts eject",
    "deploy": "npm run build && gh-pages -d build && rm -rf build && rm public/bundle.js"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
